// import jwt from 'jsonwebtoken';

// const decoDeToken = async (token) => {
//   try {
//     const decodedToken =await  jwt.verify(token, 'soumyaranjansahu');
//     return decodedToken;
//   } catch (error) {
//     console.error('Token verification failed:', error.message);
//     throw error; 
//   }
// };

// export default decoDeToken;
